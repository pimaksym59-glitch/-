# TEST_PLAN.md — План тестирования

**Уровень:** 2 (верификация). **Проверяет:** MASTER_SPEC §1–§13. **Авторитет:** MASTER_SPEC.
**Цель:** каждое проверяемое требование `R*` покрыто хотя бы одним тестом (§R13.4, traceability).

---

## Принципы

- **Offline-first** (§R2.10): вся система тестируется без сети/ключей через фейки провайдеров
  (LLM/Image/Embedding/Telegram/Metrics). Юнит- и большинство функциональных тестов не требуют
  внешних сервисов.
- **Юнит vs интеграция** (§R3.9, R12.12): чистая логика — юнит (всегда в CI); пути с БД/сетью —
  интеграционные за `RUN_INTEGRATION=1`.
- **Детерминизм:** фейки возвращают детерминированные результаты (фиксированные эмбеддинги/тексты/
  message_id) — тесты воспроизводимы.
- **Изоляция:** каждый тест создаёт свои данные, скоупленные `channel_id`; кросс-канальная утечка —
  сама по себе проверяемый фейл (§R2.6).

## Уровни и целевое покрытие

| Уровень | Префикс | Объём | Цель покрытия |
|---|---|---|---|
| Unit (чистая логика) | `T-U` | chunking, backoff, similarity-каскад, validation-gates, prompt-builder, bandit | ветви ≥90% для домена |
| Integration (БД/очередь) | `T-I` | репозитории, SKIP LOCKED, миграции-данных, RAG-поиск | ключевые пути |
| API / contract | `T-A` | эндпоинты, RBAC, isolation, коды ошибок, 202-в-очередь | все эндпоинты API_SPEC |
| Migration | `T-M` | alembic up/down, expand-contract, enum-расширения | все миграции |
| E2E scenario | `T-E` | пайплайн end-to-end на фейках | основные сценарии §R13.5 |

Общий порог строк — ≥85%; для доменных модулей (§3 domain) — выше. CI блокирует деплой при провале
любого уровня (§R12.12).

---

## Fakes (§R2.10) — обязательный инвентарь

`FakeLLMProvider` (детермин. текст + токены/стоимость), `FakeImageProvider` (детермин. байты+seed+
identity-conditioning вход принимает), `FakeEmbeddingProvider` (детермин. вектор фикс. размерности),
`FakeTelegramClient` (детермин. message_id; режимы: ok / 429+retry_after / timeout-after-send /
permanent-error), `FakeMetricsProvider`. Фабрики `get_*_provider(settings)` возвращают фейк при
отсутствии ключа.

---

## Ключевые сценарии

### Пайплайн (T-E)
- **T-E-01 happy path:** schedule → `generate_text`→`validate`→`generate_image`→`publish`→
  `collect_metrics`; пост опубликован, `message_id` сохранён, память обновлена. (§R2.5, R13.2)
- **T-E-02 continuation-chaining:** сбой `validate` → следующая стадия НЕ ставится; задача в
  retry/`needs_review`. (§R8.4)
- **T-E-03 LEAD_TIME:** `generate_text.run_at = slot−LEAD_TIME`, `publish.run_at = slot`;
  генерация не уложилась → деферал на след. окно. (§R8.5, R8.7)

### Текст/качество (T-U)
- **T-U-10 uniqueness gate:** похожесть ≥0.85 → rewrite; ≤3 попытки; ни одна не прошла → лучший +
  `needs_review`, НЕ публикуется. (§R5.6, R5.7)
- **T-U-11 rewrites≠retries:** качество-переписывания считаются отдельно от инфра-ретраев. (§R5.6, R13-#13)
- **T-U-12 hard gates блокируют publish:** язык/длина/стоп-лист/уникальность/человечность fail →
  публикация невозможна. (§R5.9)
- **T-U-13 judge отдельным вызовом:** self-review не в том же вызове генерации. (§R5.5)
- **T-U-14 dedup-каскад дёшево→дорого:** ранний выход на trigram; порядок соблюдён. (§R5.7)
- **T-U-15 few-shot K=3–5, не 500:** в промпт уходит ≤5 примеров; 500 — только сравнение векторов. (§R5.2)
- **T-U-16 prompt-builder динамический:** нет статического промпта; соблюдён `MAX_CONTEXT_TOKENS`. (§R5.3)
- **T-U-17 bandit:** пол исследования не даёт схлопывания; авто-крутит только выбор, не идентичность. (§R5.11, R11.4)

### Изображения (T-U/T-I)
- **T-U-20 diversity до генерации:** выбор сцены исключает значения из окон (30/≥3/≥2). (§R6.3)
- **T-U-21 3 механизма дедупа:** phash/метаданные/CLIP различают разные случаи. (§R6.4)
- **T-U-22 image regen cap:** ≤`IMAGE_MAX_REGEN`; исчерпан → ревью/без фото. (§R6.5)
- **T-I-23 identity-conditioning вход:** генерация получает референсы актёра; провайдер без
  поддержки отклоняется на выборе. (§R6.1)
- **T-U-24 сцена по времени слота:** набор сцен зависит от `scheduled_at`. (§R6.6)

### Telegram (T-I)
- **T-I-30 отложенная публикация нашей очередью:** Telegram не планирует; publish в момент слота. (§R7.2)
- **T-I-31 at-least-once:** timeout-after-send → `needs_review`, НЕ авто-ретрай (нет двойного постинга). (§R7.4)
- **T-I-32 429 honors retry_after:** пауза ровно на `retry_after`; задача сохранена. (§R7.5)
- **T-I-33 перманентная ошибка без ретрая:** bad token/kicked → сразу владельцу. (§R7.5)
- **T-U-34 форматтер-лимиты:** текст ≤4096, подпись ≤1024 → gate в validate. (§R7.7)
- **T-I-35 rate-limit пер-бот распределённый:** N воркеров не превышают суммарный лимит (Redis). (§R7.6, R8.9)

### Планировщик (T-I)
- **T-I-40 SKIP LOCKED:** N воркеров не берут одну задачу дважды. (§R8.10)
- **T-I-41 идемпотентность слота:** два scheduler-инстанса не создают дубль слота (unique+advisory). (§R8.10)
- **T-I-42 DLQ:** после `MAX_RETRIES` → `dead`; requeue возвращает в `pending`. (§R8.11)
- **T-U-43 backoff:** экспонента+jitter, max 1h. (§Appendix B)
- **T-U-44 DST:** несуществующий слот→сдвиг; двойной→первое вхождение. (§R8.6)

### Память/RAG (T-I)
- **T-I-50 изоляция канала hard-filter:** запрос без `channel_id` не выдаёт чужие данные;
  cross-channel утечка → фейл. (§R9.2, R2.6)
- **T-U-51 chunking семантический:** границы по смыслу, размер под окно, перекрытие. (§R9.5)
- **T-I-52 reranking hard vs soft:** channel/тип/активная-версия — фильтр до ранжирования. (§R9.7)
- **T-I-53 KB versioning:** retrieval берёт активную версию; старые сохранены. (§R9.10)
- **T-U-54 Style Memory = признаки:** инжектятся параметры, не тексты. (§R9.12)

### Аналитика/обучение (T-U/T-I)
- **T-I-60 analytics gated:** без адаптера views/ER=`null`+флаг, не выдуманные. (§R7.3, R10.3)
- **T-U-61 governance:** попытка авто-изменить стиль/персону/промпт → запрещена. (§R11.4)
- **T-U-62 A/B без сплита:** аудитория не делится; только temporal/cross-channel. (§R11.5)

### API/RBAC (T-A)
- **T-A-70 RBAC-матрица:** каждая роль × область по матрице API_SPEC; нарушение → 403.
- **T-A-71 isolation:** доступ к чужому `channel_id` → 403. (§R2.6)
- **T-A-72 секреты write-only:** ответы не содержат токенов/ключей. (§R10.4)
- **T-A-73 мутации-в-очередь → 202 {task_id}:** publish/generate/regenerate/schedule. (§R10.1)
- **T-A-74 optimistic lock:** PATCH с устаревшим `version` → 409. (§R4.2)
- **T-A-75 health:** `/health/live` vs `/health/ready` (503 при недоступной зависимости). (§R12.10)
- **T-A-76 Idempotency-Key → dedup_key:** повтор не создаёт дубль. (§R7.4)
- **T-A-77 AI Studio изоляция:** dry-run не публикует и не пишет в память. (§R10.9)

### Миграции (T-M)
- **T-M-80 up/down** всех ревизий на чистой БД.
- **T-M-81 expand-contract** на непустой горячей таблице без длительной блокировки. (§R12.6)
- **T-M-82 enum-расширения** `task_status`/`user_role` применяются. (§R4.11/R4.13)
- **T-M-83 partial unique + soft delete:** повторное создание после soft-delete допустимо. (§R4.4)

### Fail-safe (T-E)
- **T-E-90 недоступность провайдера:** материал НЕ публикуется, ошибка сохранена, уведомление,
  ретрай по политике, система не падает. (§R1.10, R2.9)
- **T-E-91 AI fallback-каскад:** primary→fallback→reschedule→уведомить. (§R5.10, R2.9)

---

## Traceability matrix (требование → тесты)

| Раздел | Ключевые R* | Тесты |
|---|---|---|
| §1 Runtime rules | R1.5–R1.10 | T-U-10..17, T-E-90 |
| §2 Architecture | R2.5,R2.6,R2.9,R2.10 | T-E-01, T-I-50, T-A-71, fakes |
| §3 Layers | R3.1,R3.7 | статанализ + T-A (нет SQL в роутере — ревью/линт) |
| §4 Data | R4.2,R4.4,R4.5–R4.8 | T-A-74, T-M-83, T-I-52 |
| §5 AI | R5.2,R5.5–R5.11 | T-U-10..17, T-E-91 |
| §6 Image | R6.1,R6.3–R6.6 | T-U-20..24, T-I-23 |
| §7 Telegram | R7.2,R7.4–R7.7 | T-I-30..35, T-U-34 |
| §8 Scheduler | R8.4,R8.5,R8.9–R8.11 | T-E-02,03, T-I-40..42, T-U-43,44 |
| §9 Memory/RAG | R9.2,R9.5,R9.7,R9.10,R9.12 | T-I-50,52,53, T-U-51,54 |
| §10 Panel | R10.1,R10.3–R10.5,R10.9 | T-A-70..73,77, T-I-60 |
| §11 Analytics | R11.4,R11.5 | T-U-61,62 |
| §12 DevOps | R12.6,R12.10,R12.12 | T-M-80..82, T-A-75, CI |
| §13 Impl | R13.2,R13.4 | T-E-01, вся матрица |

> Требования, не покрываемые автотестом (напр. R12.7 backup, R12.15 deployment) — верифицируются
> **чек-листом релиза** (§Release ниже), помечены как manual-verify.

## CI (§R12.12)
`ruff format --check` → `ruff` → `mypy` → `pytest -m "not integration"` →
`RUN_INTEGRATION=1 pytest` (staging) → build → `alembic upgrade head` на тест-БД → deploy.
Провал любого шага блокирует деплой.

## Release checklist (manual-verify, §R13.4)
☐ все сервисы стартуют · ☐ миграции проходят · ☐ основные сценарии (T-E) зелёные · ☐ юнит+интеграция
зелёные · ☐ backup настроен и **restore проверен на staging** (§R12.8) · ☐ мониторинг+алерты активны
(dead-man's-switch §R12.10) · ☐ traceability matrix без незакрытых проверяемых `R*`.

## Трассировка → MASTER_SPEC
offline/fakes §R2.10 · unit/integration §R3.9,R12.12 · traceability §R13.4 · release criteria §R13.4.
