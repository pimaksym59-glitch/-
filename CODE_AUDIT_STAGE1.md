# CODE_AUDIT_STAGE1.md — Аудит качества кода Этапа 1

**Область аудита:** структура `app/`, `pyproject.toml`, `tests/`, вспомогательные файлы Этапа 1.
**Дата:** 2026-07-22. **Метод:** self-review + инструментальная проверка (ruff/mypy/pytest).
**Дисклеймер:** на Этапе 1 «кода» в смысле бизнес-логики нет — аудируются структура, типизация,
тестируемость, соответствие Clean Architecture/SOLID и отсутствие долга.

---

## 1. Clean Architecture (§R3.1)

| Критерий | Статус | Комментарий |
|---|---|---|
| Слои выделены | ✅ | presentation/application/domain/data + infra — каждый отдельным пакетом |
| Направление зависимостей задано | ✅ | зафиксировано в docstring каждого пакета |
| Направление **проверяется автоматически** | ✅ | `tests/test_layering.py` — AST-guard, ловит нарушения на будущих этапах |
| Домен изолирован от БД/HTTP | ✅ (задекларировано) | правило в docstring + в FORBIDDEN-матрице guard'а |
| Циклов нет | ✅ | пакеты пусты; guard + ruff `TID`/`I` подстрахуют далее |

## 2. SOLID (в объёме структурного этапа)

- **SRP** — соблюдён: один пакет = одна зона ответственности (§R2.3), явно в docstring.
- **DIP** — заложен: домен будет зависеть от абстракций провайдеров (интерфейс+фабрика+фейк,
  §R2.10), пакеты `llm/`, `images/providers/` подготовлены под это.
- **OCP** — заложен: расширение провайдеров «новый файл + фабрика» без правки существующего (§R3.8).
- LSP/ISP — предметны на этапах с интерфейсами (11+); структурно не нарушаемы сейчас.

## 3. Типизация (§R3.6)

- `mypy --strict` — **0 ошибок на 26 файлах**; `disallow_untyped_defs`, `no_implicit_reexport` и др. включены.
- `app/py.typed` — маркер PEP 561 присутствует.
- Тестовый код типизирован полностью (функции `-> None`, аннотированные структуры).

## 4. Линтер / формат

- `ruff check` — **All checks passed** (правила E/F/W/I/UP/B/C4/SIM/TID/RUF).
- `ruff format --check` — все файлы отформатированы.
- Одно замечание SIM102 (вложенный `if` в guard'е) выявлено и **исправлено** в ходе этапа.

## 5. Тестируемость (§R3.9, §R2.10)

- Тесты **offline** (без БД/сети) — соответствует принципу offline-first.
- Структура `tests/` зеркалит `app/`.
- 4 теста, все зелёные; покрывают структурный инвариант и архитектурный guard.

## 6. Технический долг и «магия»

- **Долг: не обнаружен.** Нет TODO/FIXME/временных заглушек/хардкода.
- Единственная осознанная отложенность (runtime-deps declared-not-installed) документирована в
  `pyproject.toml` — это манифест, не долг.
- Пустые пакеты содержат только docstring — не заглушки-костыли, а объявленная структура.

## 7. Наблюдения и риски (не блокирующие)

| # | Наблюдение | Severity | Действие |
|---|---|---|---|
| A | venv — Python **3.14.6**, а floor спека 3.13+. 3.14 очень новый; часть runtime-зависимостей (asyncpg, pgvector, aiogram) может отставать по совместимости | 🟡 | проверить установку стека на Этапе 2–4; при проблемах — зафиксировать 3.13 |
| B | Runtime-deps объявлены, но не установлены | 🟢 | норма; ставятся на своих этапах |
| C | `legacy/` остаётся в дереве (исключён из ruff; mypy/pytest его не видят) | 🟢 | архив по решению владельца; удалить/вынести позже отдельной командой |
| D | Пустые плейсхолдер-диры держатся через `.gitkeep` | 🟢 | наполняются в своих этапах |
| E | Guard слоёв проверяет импорты статически (AST), не «открытие сессии БД» семантически | 🟢 | достаточно для направления зависимостей; семантику ловит ревью |

## 8. Трассируемость к требованиям

Затронуты и удовлетворены: **§R3.1** (слои+guard), **§R3.2** (модели — единый дом, пакет создан),
**§R3.5** (точки входа — пакеты заготовлены), **§R3.6** (типизация strict + py.typed),
**§R3.9** (tests зеркалят структуру, offline), **Appendix D(b)** (offline-импорт).
Требования этапов 2+ — вне области (пакеты пусты).

## 9. Аудит `pyproject.toml` (по запросу владельца, перед Этапом 2)

| Критерий | Итог |
|---|---|
| **Соответствие §R1.3 (стек)** | ✅ присутствует весь заявленный стек: FastAPI, uvicorn, pydantic(+settings), SQLAlchemy 2, Alembic, asyncpg, pgvector, redis, aiogram, aiohttp, anthropic, openai, pillow |
| **Лишние зависимости** | ⚠️→✅ найдена 1, **убрана**: `uuid7` — преждевременна и неоднозначна (stdlib `uuid.uuid7()` есть только в Python 3.14, а floor проекта — 3.13). Перенесена в Stage 6, выбор 3.13-vs-3.14/бэкпорт — возможно через ADR |
| **Недостающие (для Stage 1)** | ✅ нет. Будущие (argon2/bcrypt для паролей §R10.4, `python-multipart` для upload, S3-клиент для §R6.8) добавляются на своих этапах — не Stage 1 |
| **Optional/dev группы** | ⚠️→✅ **исправлено**: dev перенесён с `[project.optional-dependencies]` на **PEP 735 `[dependency-groups]`** — dev-тулинг не должен быть installable-extra в метаданных wheel (pip 26.1.2 поддерживает `--group`) |
| **Совместимость версий** | ✅ добавлены значимые floor'ы: `aiogram>=3` (v3 — async API, v2 несовместим по API), `redis>=5` (стабильный async); `pydantic>=2`, `sqlalchemy>=2` уже были |
| **Production readiness** | ⚠️ частично: манифест валиден, build/install OK. **Полное пиннинг версий + `uv.lock` — это §R12.13 (DevOps-этап)**, не Stage 1. `license` не задан — **не выдумываю**, открытый пункт для владельца |

**Применённые правки:** удалён `uuid7`; dev → `[dependency-groups]`; добавлены `aiogram>=3`,
`redis>=5`; README dev-install обновлён на `--group dev`. **Повторная верификация:** ruff/format/
mypy-strict/pytest — зелёные; `pip install --group dev --dry-run` резолвится; манифест парсится
(14 runtime-deps, dev-группа, без optional-dependencies).

**Остаточные (не блокирующие) пункты:**
- Полный пиннинг + `uv.lock` → §R12.13.
- `license` не задан → на усмотрение владельца (не фабрикую).
- UUIDv7-провайдер + выбор Python 3.13/3.14 → на Stage 6, возможно ADR (Architecture Freeze).

## 10. Вердикт

**Этап 1 — чисто.** Структура соответствует Clean Architecture и §R3, строго типизирована,
проходит линтер/формат/тесты, собирается. Технического долга нет. Готов к Этапу 2 (Конфигурация)
**после отдельного подтверждения владельца**. Наблюдение A (Python 3.14 vs стек) — первое, что
стоит проверить на Этапе 2, но Этап 1 оно не блокирует.
