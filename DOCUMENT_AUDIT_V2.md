# DOCUMENT_AUDIT_V2.md — Аудит документации после консолидации

**Дата:** 2026-07-22 · **Объём:** MASTER_SPEC.md v2.0, DATABASE_SPEC.md, API_SPEC.md, TEST_PLAN.md.
**Предшественник:** REPORT.md (аудит v1). **Статус:** self-review завершён, спецификация/код не менялись.

---

## 1. Что сделано (соответствие заданию)

| Шаг задания | Статус | Результат |
|---|---|---|
| 1. Устранить инверсию SoT | ✅ | MASTER_SPEC §0 объявлен уровнем 1; иерархия документов §R0.2; все требования и 44 решения подняты в MASTER_SPEC |
| 2. Устранить [SYNC] REPORT | ✅ | #2 (стиль→монолит §R2.1), #3/#11 (порядок: Validation до Telegram §R13.1), #4 (единый порядок), #12 (правила слоёв §R3.1), #14 (направление зависимостей §R3.1) |
| 3. Новая редакция MASTER_SPEC | ✅ | v2.0, 145 требований `R*`, §0–§13 + Appendix A–D |
| 4. DATABASE/API/TEST | ✅ | Все три созданы, уровень реализации, ссылаются на `R*` |
| 5. Self-review | ✅ | Ниже |
| 6. DOCUMENT_AUDIT_V2 | ✅ | Этот документ |

**High-пробелы REPORT ([GAP] #4–9) внесены как требования:** Telegram-ограничения (§R7.2–R7.4),
изоляция каналов (§R2.6), масштаб+rate-limiter (§R2.8/R7.6), безопасность (§R12.2), постоянство
лица (§R6.1), реалистичный self-learning (§R11.3/R11.5).

---

## 2. Результаты self-review

### 2.1 Противоречия — не обнаружено
Проверены сквозные согласования между 4 документами:
- **Порядок реализации:** Validation (§R13.1 шаг 14) до Image (15) до Telegram (16) — согласуется с
  validation-gate (§R5.9). ✔
- **Enums:** `task_type` (9), `task_status` (8), `user_role` (5) идентичны в MASTER §R4.11–13,
  DATABASE (CREATE TYPE) и API (RBAC-матрица). ✔
- **Размерности векторов:** N_text=1536, N_clip=512, N_face=512 — консистентны во всех
  vector-колонках DATABASE и §R4.5/R4.6. ✔
- **Параметры:** пороги (0.85 / 75 / окна / regen / retries) совпадают в §Appendix B и
  `channel_settings`. ✔ (см. п.4.3 — коупл-риск)
- **Роли/RBAC:** 5 ролей MASTER = DATABASE enum = API-матрица. ✔

### 2.2 Полнота требований — достаточна для старта
145 `R*` покрывают §1–§13; все 44 решения ledger подняты (Appendix A); acceptance criteria
(Appendix D). Проверяемые требования отражены в TEST_PLAN traceability-матрице; непокрываемые
автотестом (backup/deployment) — в release-чеклисте (manual-verify). ✔

### 2.3 Дублирование — контролируемо
Принцип §R0.2 соблюдён: **требования — только в MASTER**, детальные документы содержат
реализацию, ссылаясь на `R*`, а не переопределяя. Остаточная связка — п.4.3.

### 2.4 Корректность ссылок — подтверждена машинно
- Определено `R*` в MASTER_SPEC: **145**.
- Ссылок `R*` из DATABASE/API/TEST на несуществующие требования: **0**.
- Обратные ссылки (детальные → MASTER §R*) и прямые (MASTER §4 → DATABASE_SPEC; §10 → API_SPEC;
  §13 → TEST_PLAN) — взаимны. ✔

### 2.5 Готовность к реализации — да, с известными отложенностями
Документация достаточна, чтобы начать по §R13.1 с шага 1. Отложенности зафиксированы (п.4) и
**не блокируют** ранние шаги (структура, конфиг, Docker, БД, очередь).

---

## 3. Открытые вопросы — за владельцем (блокируют отдельные модули, не старт)

| # | Вопрос | Дефолт (действует до решения) | Влияет на |
|---|---|---|---|
| O-1 | **MTProto read-only stats-адаптер** (да/нет) | **нет** → per-post views/ER недоступны, self-learning слабый | §5, §10, §11, §12 |
| O-2 | **Целевая среда развёртывания** (VM/облако/оркестрация) | **одна VM + Compose + Caddy** | §12 |

Оба уже зафиксированы в MASTER_SPEC §Appendix C. Реализация ядра (§1–§9) от них не зависит; они
всплывают на §11 (обучение/аналитика) и §12 (инфра).

---

## 4. Технические открытые вопросы — за инженером (уточнить до соответствующего модуля)

| # | Вопрос | Рекомендация | Когда нужно |
|---|---|---|---|
| T-1 | **Конкретная embedding-модель** (N_text=1536 подразумевает OpenAI `text-embedding-3-small`) — платформенная константа §R4.6 | подтвердить модель/провайдера до реализации Memory/RAG | шаг 13 (Memory) |
| T-2 | **CLIP/face модели** (512) — конкретные веса для §R6.7 | выбрать при реализации Image validator | шаг 15 (Image) |
| T-3 | **Identity-conditioning провайдер** (§R6.1) — какой реально поддерживает | доказать на прототипе до Image Engine | шаг 15 |
| T-4 | **JSON-схемы тел запросов/ответов** API | генерируются как OpenAPI из `app/schemas` (Pydantic) — заложено в API_SPEC | шаг 10 (API) |
| T-5 | **Style-feature экстрактор** (сложные признаки §R9.12) | старт с измеримых признаков; арка/переходы — позже | шаг 13 |

---

## 4.3 Замечания качества документации (не блокирующие)

- **Коупл-риск дефолтов:** значения параметров (§Appendix B) продублированы как DEFAULT в
  `channel_settings` (DATABASE). Источник истины — **§Appendix B**; при изменении править обе точки
  (либо генерировать миграцию из Appendix B). Задокументировано, не противоречие.
- **LEAD_TIME:** MASTER §Appendix B задаёт диапазон «30–60 мин», DATABASE конкретизирует дефолт
  **45**. Значение внутри диапазона — согласовано; при желании зафиксировать одно число в §Appendix B.
- **`docs/spec/01–13`** теперь уровень 3 (справочный). Рекомендуется добавить в их шапки пометку
  «superseded by MASTER_SPEC v2.0 as authority» во избежание путаницы (doc-hygiene, требует
  отдельного согласования — §R0.4).

---

## 5. Вердикт

**Документация готова к началу реализации.** Инверсия SoT устранена, [SYNC] закрыты, High-пробелы
внесены как требования, 4 документа взаимно согласованы (0 битых ссылок), verifiable-требования
трассируются в тесты.

- **Блокирующих** открытых вопросов для старта (§R13.1 шаги 1–12) — **нет**.
- **Отложенные** решения (O-1, O-2 — за владельцем; T-1…T-5 — за инженером) всплывают на конкретных
  поздних шагах и имеют дефолты/рекомендации.

**Следующее действие — на твоё подтверждение** (не начинаю без него): (а) начать реализацию с
§R13.1 шаг 1; или (б) сначала закрыть O-1/O-2; или (в) внести doc-hygiene пометки в `docs/spec`.

---

*Спецификация не изменялась (кроме исправления косметической пустой ссылки R5.8). Код не генерировался.*
