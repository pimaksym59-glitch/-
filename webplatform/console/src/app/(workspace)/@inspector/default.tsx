import { InspectorEmpty } from '@/widgets/inspector';

/** Default state of the Universal Inspector parallel slot (nothing selected). */
export default function InspectorDefault(): React.ReactElement {
  return <InspectorEmpty />;
}
