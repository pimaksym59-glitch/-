/** Public API — widgets/memory (FS8). Detail panes, the edit dialog and the AI
 * panel are lazy leaves reached through MemoryView; only the shell is public. */
export { MemoryView, type MemoryInitial } from './MemoryView';
export { MemoryHonesty } from './MemoryHonesty';
