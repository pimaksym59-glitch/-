/** Public API — feature `explain-style` (FS8 persona-provenance AI). */
export { ExplainStylePanel, personaSummary } from './ui/ExplainStylePanel';
export {
  buildPersonaPrompt,
  EXPLAIN_QUESTION,
  type PersonaPrompt,
} from './model/buildPersonaPrompt';
