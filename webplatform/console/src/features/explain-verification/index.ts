/** Public API — feature `explain-verification` (FS9 image-provenance AI). */
export { ExplainVerificationPanel, imageSnippet } from './ui/ExplainVerificationPanel';
export {
  buildImagePrompt,
  EXPLAIN_IMAGE_QUESTION,
  type ImagePrompt,
} from './model/buildImagePrompt';
