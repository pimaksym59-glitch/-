/** Public API — feature `test-prompt` (FS10; the owner-approved D8 surface:
 *  a contract-only dry-run of ONE version, never prompt authoring). */
export { TestPromptPanel, versionSnippet } from './ui/TestPromptPanel';
export { buildPromptRun, DEFAULT_SAMPLE_INPUT, type PromptRun } from './model/buildPromptRun';
