/** Public API — feature `explain-job` (FS12, the stage's ONE AI surface). */
export { buildJobPrompt, EXPLAIN_JOB_QUESTION, type JobPrompt } from './model/buildJobPrompt';
export { ExplainJobPanel, taskSnippet } from './ui/ExplainJobPanel';
