/**
 * entities/ — domain objects + hooks/mappers/ui (Stage 3 §4). FS4 lands the
 * first slice: `session` (read side; the write side is feature `auth`).
 * Import slices directly (`@/entities/session`) — this file is documentation,
 * not a barrel.
 */
export {};
